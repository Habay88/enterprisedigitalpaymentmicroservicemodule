<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.edpp</groupId>
        <artifactId>enterprise-digital-payment-platform</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>api-gateway</artifactId>
    <packaging>jar</packaging>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2023.0.0</spring-cloud.version>
    </properties>

    <dependencies>
        <!-- 
            Spring Cloud Gateway - The core dependency for API Gateway functionality.
            Provides reactive programming model, routing, filtering, and load balancing.
            Unlike traditional Servlets, it uses Netty and WebFlux for non-blocking I/O.
        -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway</artifactId>
        </dependency>

        <!-- 
            Circuit Breaker - Provides fault tolerance for downstream services.
            Prevents cascading failures when a service is down.
            Implements patterns like Circuit Breaker, Retry, Bulkhead, and Rate Limiter.
        -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-circuitbreaker-reactor-resilience4j</artifactId>
        </dependency>

        <!-- 
            Load Balancer - Client-side load balancing for service discovery.
            Distributes requests across multiple instances of a service.
            Works with Eureka for dynamic service discovery.
        -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
        </dependency>

        <!-- 
            Eureka Client - Service discovery client.
            Allows the gateway to discover service locations dynamically.
            Eliminates hardcoded URLs and enables horizontal scaling.
        -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
        </dependency>

        <!-- 
            Actuator - Production-ready monitoring endpoints.
            Provides health checks, metrics, and info endpoints.
            Essential for container orchestration and monitoring.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- 
            Redis Reactive - For distributed rate limiting.
            Stores rate limit counters in Redis for consistency across multiple gateway instances.
            Reactive version supports non-blocking operations.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis-reactive</artifactId>
        </dependency>

        <!-- 
            Spring Security - Authentication and authorization framework.
            Protects API endpoints and validates JWT tokens.
            Integrates with WebFlux for reactive security.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- 
            JJWT - Java JWT library for token creation and validation.
            Creates and parses JWT tokens for authentication.
            Supports signing with HMAC or RSA keys.
        -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.11.5</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.11.5</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.11.5</version>
            <scope>runtime</scope>
        </dependency>

        <!-- 
            Resilience4j - Fault tolerance library.
            Provides circuit breakers, retries, rate limiters, and bulkheads.
            More lightweight than Hystrix with better reactive support.
        -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
        </dependency>

        <!-- 
            Micrometer Tracing - Distributed tracing support.
            Adds trace IDs and span IDs to requests for debugging.
            Integrates with Zipkin for trace visualization.
        -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-brave</artifactId>
        </dependency>

        <!-- 
            Lombok - Reduces boilerplate code.
            Generates getters, setters, builders, and constructors at compile time.
        -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Testing dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>